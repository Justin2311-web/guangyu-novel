export type Role = 'superadmin' | 'admin' | 'author' | 'reader';

export type NovelStatus = 'draft' | 'pending_review' | 'published' | 'rejected' | 'archived';

export type ChapterStatus = 'draft' | 'pending_review' | 'published' | 'rejected';

export const PLACEHOLDER = 'Phase 1 will introduce Supabase types and a typed client.';
