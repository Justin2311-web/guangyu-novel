# Copy to .env.local in each app that needs these.
# Phase 1+ will populate these — Phase 0 does not require any env vars.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
